/*import React, { useState } from 'react';

const Contador = () => {

    const [email, setEmail] = useState(0);
    const [password, setPassword] = useState(0);

    const loginUser = () => {
        console.log(email);
        console.log(password);
    }

    return ( 
        <div>
            <h3>Mi primer componente</h3>
            <input type="email" placeholder="E-mail" onChange={e => setEmail(e.currentTarget.value)}/>
            <input type="password" placeholder="Pass" onChange={e => setPassword(e.currentTarget.value)}/>
            <button onClick={loginUser}>ingresar</button>
        </div>
    );
}
 
export default Contador;
*/